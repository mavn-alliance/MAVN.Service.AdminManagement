apiVersion: v1
kind: Service
metadata:
  labels:
    app: admin-management
    environment: Live
  name: admin-management
  namespace: services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: admin-management
  sessionAffinity: None
  type: ClusterIP
